---
import { cn, cardVariants, type CardProps } from "../../lib/utils";

export interface Props extends CardProps {
  class?: string;
}

const { variant, size, class: className, ...props } = Astro.props;
---

<div
  class={cn(cardVariants({ variant, size }), className)}
  {...props}
>
  <slot />
</div>