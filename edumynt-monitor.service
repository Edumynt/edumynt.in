[Unit]
Description=Edumynt Site Monitor
After=network.target nginx.service

[Service]
Type=oneshot
ExecStart=/bin/bash -c 'if ! curl -f -s http://localhost > /dev/null; then systemctl restart nginx; fi'
User=ubuntu

[Install]
WantedBy=multi-user.target